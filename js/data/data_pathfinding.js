data.pathfinding = {
  projection: {
    iterations: 14,
    timestretch: 15, //used to compute values further in time, each iteration takes this many frames, therefore allowing you to compute many seconds ahead
  }
}